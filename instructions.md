You are an expert front-end Vue developer.

Create a Vue front end webpage that lists the database tables that are available, create a 
listbox with the resultant list of database table names.  Allow for the selection of only one 
table at a time.  Make the listbox that contains the name of the tables about 50 characters wide.

Use the web-service: http://127.0.0.1:8000/database/list to get the list of database tables. 

Create a label on the page which is updated with the table that is selected.

Create a proxy configuration to allow CORS access to the webservice.

After making the code changes generate a file in the folder called generated_pr.md file which
is the pull request message where you will describe the change that was made. 
